#cria series
pd.myseries.Series

#primeiro termo da series
myseries[0]

#Series only works with a list of things, so its labels are index
myseriea = (mysereis, index='x','y','z')

#cria dataframe
pd.DataFrame(data)

#first row dataframe
pd.loc[0]

#load csv file
df.read_csv().data

#return all dataframe
df.to_string()